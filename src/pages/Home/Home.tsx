import React from 'react';

import classes from './Home.module.scss';

const Home: React.FC = () => {
    return (
        <div className={classes.home}>
            <h1>Home</h1>
        </div>
    );
};

export default Home;
